# Simply Wall St forecast scraper — deployment dependencies only.
# (Browser/OCR/LLM/database deps from the old multi-country pipeline removed.)

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
curl_cffi>=0.7        # impersonates Chrome to get past Cloudflare
openpyxl>=3.1.2       # Excel export for /api/simply/excel
yfinance>=0.2.40      # beta lookup for /api/beta
